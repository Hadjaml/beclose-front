# Public API base URL — never a secret, safe to expose in the client bundle
# (AGENTS.md, "Backend-driven capabilities"). Defaults to Beclose's local
# uvicorn address (http://localhost:8000) when unset — see
# src/shared/api/backend-client.ts.
NEXT_PUBLIC_API_BASE_URL=http://localhost:8000
