# Agent resources

The repository-wide coding and architecture rules live in the root
[`AGENTS.md`](../AGENTS.md) file. It remains the single source of truth for all
coding agents working on Bewise.

Use this directory only for future supporting resources that are too detailed
for `AGENTS.md`, such as review checklists or focused implementation guides.
Supporting documents must not duplicate or override the root rules.
